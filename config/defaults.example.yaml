project:
  default_locale: "ru"

providers:
  llm: "stub"
  embeddings: "stub"

sources:
  allowed_formats: ["pdf", "docx", "md", "xlsx", "csv"]
  max_file_size_mb: 50

retrieval:
  strategy: "to_be_decided"

runtime:
  log_level: "INFO"
  offline_by_default: true

prompts:
  root: "prompts"

security:
  secrets_source: "environment"
